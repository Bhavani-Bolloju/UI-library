{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html"
  },
  "devDependencies": {
    "lerna": "^8.1.2",
    "parcel": "latest"
  }
}
